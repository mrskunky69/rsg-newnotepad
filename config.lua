Config = {
    Debug = false, -- Set to true to enable debug messages
    Prop = "p_paper02x", -- Example prop model, replace with your desired model

    -- Model loading parameters
    ModelLoadTimeout = 5000, -- Timeout duration in milliseconds (e.g., 5000 for 5 seconds)
    ModelLoadAttemptInterval = 100 -- Interval between model loading attempts in milliseconds (e.g., 100 for 0.1 seconds)
}
